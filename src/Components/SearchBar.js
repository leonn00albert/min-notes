export const SearchBar = () => {
    return (
        <div className="card search-bar">
             <i className="fa fa-search"></i>
            <input type="text" placeholder="Start entering words for search" />
        </div>
    );
}